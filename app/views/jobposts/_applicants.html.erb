<% if user_signed_in? && current_user.applicants %>
<%= link_to "cancel", jobpost_applicants_path(@jobpost), method: :delete, remote: true %>
<% else %>
<%= link_to "Apply", new_jobpost_applicants_path(@jobpost), method: :post, remote: true%>
<% end %>
